<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.project_map.HomeFragment"
        android:label="Beranda"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_simpanFragment"
            app:destination="@id/simpanFragment"/>
    </fragment>

    <fragment
        android:id="@+id/simpanFragment"
        android:name="com.example.project_map.SimpanFragment"
        android:label="Simpanan"
        tools:layout="@layout/fragment_simpan" />
</navigation>
